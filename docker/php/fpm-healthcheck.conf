; FPM-Ping-Endpunkt für den Container-Healthcheck (compose.yaml). Nur intern
; über den FastCGI-Port 9000 erreichbar – nginx routet /ping nicht dorthin.
; FPM ping endpoint for the container health check (compose.yaml). Reachable
; only internally via FastCGI port 9000 – nginx does not route /ping there.
[www]
ping.path = /ping
ping.response = pong
